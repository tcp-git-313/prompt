# P3.2B — Resume Admin Seat Listing Read Contract

ROLE: COMMERCIAL CORE ADMIN SEAT READ CONTRACT OWNER

GOAL:
Resume P3.2 now that the Live Staging read path and same-effective-row SQL proof are complete.

VERIFIED PREREQUISITES:

P3.2A:
LIVE_STAGING_READ_PATH_RECOVERED = YES
STAGING_PROJECT_IDENTITY_VERIFIED = YES
CENTRAL_SEAT_FUNCTIONS_VISIBLE = YES

P3.1B:
LIVE_STAGING_VERIFIED = YES
OLD_CONTRACT_PRESERVED = YES
SAME_EFFECTIVE_ROW_PROVEN = YES
SEAT_LIMIT_FROM_EFFECTIVE_ROW = YES
LIVE_STAGING_CANONICAL_SQL_VERIFIED = YES
P3_1_STAGING_SQL_BLOCKER_CLEARED = YES

STAGING PROJECT:
jlsqjvehwblkeuycjoyj

CANONICAL REPO:
F:\00-Ticenpi-SaaS\ticenpi-platform

LAUNCHER REFERENCE ONLY:
F:\00-Ticenpi-SaaS\Ticenpi-Launcher

DO NOT modify Post.
DO NOT modify DM.
DO NOT implement Launcher UI.
DO NOT touch Production.
DO NOT redesign Central Seat semantics.

## 1. Preflight

Record:
- Platform branch
- Platform HEAD
- git status --short
- git diff --stat
- Launcher branch/HEAD/status for reference only

Preserve dirty WIP.
Do not reset/clean/stash/rebase.

Verify the existing Central Seat migration remains unchanged:

F:\00-Ticenpi-SaaS\ticenpi-platform\supabase\migrations\20260923_000600_central_product_seats.sql

Expected SHA-256:
32EED072E422F576C9F5612BDD2E6E33B2F265A4D777B9BAC44AC2BE9A7B62CB

If SHA differs, STOP.

## 2. Required capability

Add the smallest canonical platform-admin READ contract that lets Launcher Workbench list Product Seat assignments for one customer/product.

Required use case:

Given:
- p_customer_id uuid
- p_product_code text

Return enough information for Workbench to display:
- effective entitlement identity/status
- effective seat_limit
- assigned_count
- capacity/full state
- current assigned Seat rows
- assignment id
- assigned user_id
- assigned timestamp if stored
- canonical commercial member status if safely derivable

Do not return unnecessary auth/private data.

Do not expose email/name if existing admin_customer_detail() can merge them by user_id.

## 3. Preferred RPC

Unless actual repo conventions strongly require another name, implement:

admin_list_product_seats(
  p_customer_id uuid,
  p_product_code text
)

Before finalizing, inspect existing canonical admin RPC naming/return conventions and follow them.

Do not create a second Seat authority.

## 4. Canonical sources

The RPC must derive:

Assignments:
public.product_seat_assignments

Seat limit / effective entitlement:
the SAME resolve_effective_entitlement_core(uuid) already verified in Live Staging

Membership state:
public.customer_members where needed

Authorization:
is_platform_admin() or the exact existing canonical platform-admin server-side check

Do NOT:
- accept caller-supplied admin booleans
- use service_role in Launcher/browser
- grant direct SELECT on product_seat_assignments
- calculate seat_limit using MAX/SUM/separate ordering
- change assign_product_seat()
- change release_product_seat()
- change product_seat_status()

## 5. Security contract

If SECURITY DEFINER is used:
- explicit safe search_path
- revoke PUBLIC
- grant only the existing appropriate authenticated execution role
- server-side platform_admin authorization
- customer/product scope enforced inside RPC

Required denials:
- anonymous
- normal authenticated non-admin
- cross-customer data
- cross-product data

No new direct table read grants.

## 6. Return shape

Use the repo's established JSON/table-return convention.

The logical contract must support:

customer_id
product_code
effective_entitlement_id
effective_status
seat_limit
assigned_count
is_full

assignments:
- assignment_id
- user_id
- assigned_at/created_at if stored
- member_status if canonical and safe

If zero assignments:
- assigned_count = 0
- assignments = empty
- seat_limit/effective status still reflect the selected effective entitlement if present

Define explicit behavior when:
- customer not found
- product not found
- no effective entitlement
- invalid product code
- caller unauthorized

Keep semantics consistent with existing admin RPC conventions.

## 7. Migration

Create ONE additive migration in the canonical flat chain.

Do not rename/squash/rewrite existing migrations.

Follow current timestamp naming convention.

Add rollback according to existing repo convention.

The new migration should only add the admin read contract and required grants/revokes/comments/tests support.

Do not alter existing Central Seat table/RPC behavior.

## 8. Documentation

Update only required canonical docs:

docs/commercial-core/seats.md
docs/commercial-core/contracts/CENTRAL-SEAT-CONTRACT.md
and/or the minimal relevant Commercial Core README/reference

Document:
- RPC name
- parameters
- authorization
- return contract
- data sources
- Launcher consumption rule
- no direct client Seat table access

Do not rewrite unrelated docs.

## 9. Local tests

Add local regression tests for:

1. platform_admin can list
2. normal authenticated user denied
3. anonymous denied
4. customer isolation
5. product isolation
6. zero assignments
7. partial assignments
8. full capacity
9. effective seat_limit comes from selected resolver row
10. assigned_count equals actual assignment rows
11. Post assignment does not appear in DM query
12. suspended/removed membership behavior matches canonical Seat lifecycle
13. no new direct client SELECT on product_seat_assignments
14. assign/release/status existing behavior unchanged

Run existing Commercial Core regression/repro tests.

## 10. Staging apply

Only after local tests PASS:

Apply ONLY the new additive migration to Staging project:
jlsqjvehwblkeuycjoyj

Use the already-recovered supported Staging connection/migration path.

Do not apply unrelated pending migrations.

Do not touch Production.

If migration tooling would apply unrelated migrations or cannot isolate this migration:
STOP and report BLOCKED.

## 11. Staging verification

Use synthetic data only.

Verify:

ADMIN_LIST_PASS = YES
UNAUTHORIZED_DENIED = YES
ANONYMOUS_DENIED = YES
CUSTOMER_ISOLATION = PASS
PRODUCT_ISOLATION = PASS
ZERO_ASSIGNMENT_STATE = PASS
PARTIAL_ASSIGNMENT_STATE = PASS
FULL_CAPACITY_STATE = PASS
SEAT_LIMIT_SAME_EFFECTIVE_ROW = PASS
ASSIGN_RELEASE_LIST_CONSISTENCY = PASS
NO_DIRECT_TABLE_READ_GRANT = PASS

Use unique markers.
Record pre-test residue.
Cleanup in failure/finally path.
Prove zero residue after test.

Do not use real customers/users.

## 12. Launcher future contract

Do NOT implement UI.

Document exactly how Launcher should call the new RPC using the signed-in platform_admin session.

State:

RPC name
parameters
response fields
error cases
how to merge assignment user_id with existing admin_customer_detail() member data

No service-role key.
No direct table read.

## 13. Central Seat integrity

Recheck after migration:

ORIGINAL_SEAT_MIGRATION_SHA
EXPECTED_SHA
MATCH

And prove:

ASSIGN_SEMANTICS_CHANGED = NO
RELEASE_SEMANTICS_CHANGED = NO
STATUS_SEMANTICS_CHANGED = NO
EFFECTIVE_RESOLVER_CHANGED = NO

## 14. Commit / push

Do NOT commit or push unless explicitly instructed.

Leave changes reviewable.

## 15. Final report

### A. PREFLIGHT

### B. NEW READ CONTRACT

RPC:
Parameters:
Return:
Authorization:
Assignment source:
Seat limit source:

### C. FILES CHANGED

### D. LOCAL TESTS

### E. STAGING

MIGRATION_APPLIED =
ADMIN_LIST_PASS =
UNAUTHORIZED_DENIED =
ANONYMOUS_DENIED =
CUSTOMER_ISOLATION =
PRODUCT_ISOLATION =
ZERO_ASSIGNMENT_STATE =
PARTIAL_ASSIGNMENT_STATE =
FULL_CAPACITY_STATE =
SEAT_LIMIT_SAME_EFFECTIVE_ROW =
ASSIGN_RELEASE_LIST_CONSISTENCY =
NO_DIRECT_TABLE_READ_GRANT =
CLEANUP_ZERO_RESIDUE =

### F. CENTRAL SEAT INTEGRITY

ORIGINAL_SEAT_MIGRATION_SHA =
EXPECTED_SHA =
MATCH =
ASSIGN_SEMANTICS_CHANGED = NO
RELEASE_SEMANTICS_CHANGED = NO
STATUS_SEMANTICS_CHANGED = NO
EFFECTIVE_RESOLVER_CHANGED = NO

### G. LAUNCHER FUTURE CALL CONTRACT

### H. REMOTE SAFETY

PRODUCTION_MUTATED = NO
POST_MODIFIED = NO
DM_MODIFIED = NO
LAUNCHER_UI_MODIFIED = NO

### I. FINAL RESULT

If complete:

RESULT = PASS
ADMIN_SEAT_LISTING_READ_CONTRACT = PASS
STAGING_READY = YES
P3_BLOCKER_3_CLEARED = YES
READY_FOR_LAUNCHER_SEAT_UI = YES
READY_FOR_PHASE_4_STAGING_SHADOW = YES

If blocked:

RESULT = BLOCKED
ADMIN_SEAT_LISTING_READ_CONTRACT = BLOCKED
P3_BLOCKER_3_CLEARED = NO
BLOCKER = exact factual blocker

Finish this task and stop.
