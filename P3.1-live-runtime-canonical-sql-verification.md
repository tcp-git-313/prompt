# P3.1 — Live Runtime & Canonical SQL Verification

ROLE: CENTRAL SEAT PRE-CUTOVER VERIFICATION OWNER

GOAL:
Clear Phase 3 blockers #1 and #2 before Post/DM implementation:
1. Prove effective Post runtime authorization flags in Staging and Production using read-only runtime evidence.
2. Prove Live Staging Central Seat SQL/signatures and same-effective-row behavior using a controlled synthetic Staging fixture with guaranteed cleanup.

REPOS:
POST = F:\00-Ticenpi-SaaS\TicenpiPost
PLATFORM = F:\00-Ticenpi-SaaS\ticenpi-platform

CANONICAL OWNER:
ticenpi-platform

DO NOT modify product source code.
DO NOT modify Production.
DO NOT deploy.
DO NOT commit or push.
DO NOT change Production env/config.
DO NOT expose secrets.

Production is READ-ONLY only.

Staging is READ-ONLY except for the explicitly allowed temporary synthetic verification fixture described below.

## A. Preflight

Record for Post and Platform:
- branch
- HEAD
- git status --short
- git diff --stat

Do not clean dirty work.

## B. Effective Post runtime flags

Prove the actual effective values, not templates, for:

TICENPI_COMMERCIAL_GATE_ENABLED
TICENPI_AUTO_TENANT_BOOTSTRAP
TICENPI_SEAT_POLICY
TICENPI_ALLOW_TENANT_KEY

For:
- Local/root runtime
- Staging runtime
- Production runtime

Preferred evidence:
- running container environment / effective orchestrator environment
- deployed release compose/env identity
- runtime-config only if it is the actual effective source

Do not print unrelated environment variables.
Do not print secrets.
If a value cannot be proven, report UNKNOWN.

For each environment report:
- host/release/container identity
- exact four non-secret effective values
- evidence source
- whether Seat can currently be bypassed

Do not restart or modify containers.

## C. Live Staging canonical SQL verification

Against Staging only, verify current live definitions/signatures for:

resolve_effective_entitlement_core(uuid)
effective_entitlements(uuid)
effective_entitlement_details(uuid)
product_seat_status(text)
assign_product_seat(uuid,text,uuid)
release_product_seat(uuid,text,uuid)

Verify effective_entitlements remains:
TABLE(product_code text, status text)

Verify effective_entitlement_details returns:
product_code,status,seat_limit,entitlement_id

Verify source/definition shows both accessors use the shared resolver.

Verify Central Seat migration SHA in repo remains:
32EED072E422F576C9F5612BDD2E6E33B2F265A4D777B9BAC44AC2BE9A7B62CB

Do not change live SQL definitions.

## D. Controlled same-effective-row Staging fixture

This is the ONLY allowed remote mutation.

Use Staging only.
Never Production.

Use synthetic uniquely identifiable test records.
Reuse the existing verified Staging verification/repro tooling where safe rather than inventing a second test harness.

Create one customer/product context with two effective entitlement candidates such that the existing ordering is deterministic:

A:
- active
- seat_limit = 2
- older updated_at

B:
- trial
- seat_limit = 5
- newer updated_at

The existing resolver rule must select B.

Prove on LIVE STAGING:

effective_entitlements(customer)
=> same product_code + status from B

effective_entitlement_details(customer)
=> same product_code + same status from B
=> seat_limit = 5
=> entitlement_id = B.id

Explicitly prove:
- result is not 2 + 5
- result is not MAX(seat_limit) independent of selected row
- old and detailed accessor point to the same effective row

Do not modify resolver logic to make the test pass.

## E. Fixture safety

Before mutation:
- identify exact synthetic records to be created
- record pre-test residue count for the unique test marker

During test:
- do not touch real customers/users/entitlements
- do not use real user email/data
- do not modify Production

Cleanup must run in finally/failure path.

After cleanup prove zero residue for:
- synthetic customer
- synthetic member/test identity if created
- synthetic subscription/entitlement rows
- synthetic Seat assignments
- related synthetic audit rows when safely attributable

If guaranteed cleanup cannot be performed:
STOP before creating the fixture.

## F. Interpretation

For each runtime environment classify:

FORMAL_SEAT_ENFORCEMENT_SAFE = YES/NO/UNKNOWN

A formal environment is NOT safe if any actual effective path permits:
- Commercial Gate disabled
- Seat policy skip
- Seat checker omitted because bootstrap flag is off
- X-Tenant-Key bypass around JWT/commercial/Seat

Do not change the environment in this task.
Only report exact required correction for Phase 4.

## G. Safety

Must end with:

SOURCE_FILES_MODIFIED = NO
SQL_DEFINITION_MODIFIED = NO
PRODUCTION_MUTATED = NO
PRODUCTION_CONFIG_CHANGED = NO
STAGING_SCHEMA_CHANGED = NO
STAGING_TEMP_FIXTURE_CREATED = YES/NO
STAGING_TEMP_FIXTURE_CLEANED = YES/NO
COMMIT_CREATED = NO
PUSHED = NO
DEPLOYED = NO

## H. Final report

A. PREFLIGHT

B. EFFECTIVE RUNTIME FLAGS
Table:
Environment | Commercial Gate | Auto Bootstrap | Seat Policy | Tenant Key | Effective evidence | Seat bypass risk

C. LIVE STAGING SQL
Function | Live signature | Shared resolver evidence | PASS/FAIL

D. SAME-EFFECTIVE-ROW PROOF
A entitlement id/status/limit/updated_at
B entitlement id/status/limit/updated_at
effective_entitlements result
effective_entitlement_details result
SAME_EFFECTIVE_ROW = YES/NO
SEAT_LIMIT_FROM_SELECTED_ROW = YES/NO

E. CLEANUP
Residue counts before/after

F. BLOCKERS
Exact remaining blockers only.

G. FINAL RESULT

If all required evidence is proven:

RESULT = PASS
LIVE_STAGING_CONFIG_VERIFIED = YES
LIVE_PRODUCTION_CONFIG_VERIFIED = YES
LIVE_STAGING_CANONICAL_SQL_VERIFIED = YES
SAME_EFFECTIVE_ROW_PROVEN = YES
P3_BLOCKERS_1_2_CLEARED = YES

If Production runtime cannot be proven:

RESULT = PARTIAL_PASS
LIVE_STAGING_CONFIG_VERIFIED = YES/NO
LIVE_PRODUCTION_CONFIG_VERIFIED = NO
LIVE_STAGING_CANONICAL_SQL_VERIFIED = YES/NO
SAME_EFFECTIVE_ROW_PROVEN = YES/NO
P3_BLOCKERS_1_2_CLEARED = NO
BLOCKER = exact missing evidence

Do not fix runtime flags.
Do not implement Post/DM shadow.
Verify and stop.
