# P3.2 — Platform Admin Seat Listing Read Contract

ROLE: COMMERCIAL CORE ADMIN SEAT READ CONTRACT OWNER

GOAL:
Clear Phase 3 blocker #3 by adding the smallest safe canonical READ contract needed for Launcher Workbench to list assigned product Seats for one customer/product.

CANONICAL REPO:
F:\00-Ticenpi-SaaS\ticenpi-platform

LAUNCHER REFERENCE ONLY:
F:\00-Ticenpi-SaaS\Ticenpi-Launcher

CANONICAL RUNTIME:
Central Supabase

This is an additive Commercial Core change.
No Product Seat rule redesign.
No Post/DM changes.
No Launcher UI implementation in this task.
No Production mutation.

## A. Preflight

Record:
- Platform branch/HEAD/status
- Launcher branch/HEAD/status

Preserve dirty WIP.
Do not reset/clean/stash/rebase.

Inspect:
docs/commercial-core/
supabase/migrations/
existing admin RPC naming/security conventions
admin_customer_detail()
admin_list_customers()
Central Seat contract
product_seat_assignments security/RLS/grants
customer_members
audit patterns

## B. Required capability

Launcher Workbench needs a platform-admin authorized read contract for one:
customer_id + product_code

It must provide enough data to display:
- assigned users
- assignment identity
- assigned timestamp if stored
- assigned Seat count
- effective seat_limit
- capacity/full state
- relevant member status if safely available from existing commercial tables

Prefer joining assignment user_id to the already-approved customer/member read model rather than exposing Auth internals.

Do not expose service-role secrets.
Do not grant direct client SELECT on product_seat_assignments merely to make the UI easy.

## C. Minimal contract rule

Choose the smallest additive RPC/read accessor consistent with existing naming conventions.

Do not invent a parallel Seat authority.

The read contract must derive:
- assignment rows from public.product_seat_assignments
- effective seat_limit from the SAME effective entitlement resolver used by Central Seat
- customer/product scope from explicit parameters
- authorization server-side via is_platform_admin() or the existing equivalent canonical admin check

Do not accept caller-supplied role/admin boolean.

Do not alter assign_product_seat(), release_product_seat(), or product_seat_status() semantics.

## D. Security requirements

The new read contract must:
- be callable only by authorized platform admin context
- use explicit safe search_path if SECURITY DEFINER is used
- avoid direct table grants that bypass the RPC boundary
- reject unauthorized authenticated users
- reject anonymous use
- not expose unrelated customers/products
- preserve current RLS/security model
- not use service_role in Launcher/browser

Verify current project conventions before choosing SECURITY DEFINER / grants.

## E. Output contract

Do not hardcode this exact shape without inspection, but the resulting read model must support the Workbench need with one coherent canonical contract.

At minimum the final documented fields must cover:
- customer_id
- product_code
- effective entitlement id or traceable effective-row identifier if appropriate
- seat_limit
- assigned_count
- capacity/full state
- assignment_id or stable assignment identifier
- assigned user_id
- assigned_at/created_at if stored
- member status if available from canonical commercial membership data

Avoid returning sensitive fields not needed by Workbench.

If display email/name is already available from admin_customer_detail(), prefer client-side merge by user_id rather than broadening this RPC unnecessarily.

## F. Migration

Follow Platform's existing flat chronological Supabase migration convention.

Create one additive migration only if required.
Create rollback following existing conventions.

Do not squash or rewrite prior migrations.

Do not modify:
20260923_000600_central_product_seats.sql

Its SHA must remain:
32EED072E422F576C9F5612BDD2E6E33B2F265A4D777B9BAC44AC2BE9A7B62CB

Update Commercial Core docs/contracts only as required to document the new admin read contract.

## G. Tests

Add local regression coverage for:
- platform_admin can list
- normal authenticated user denied
- anonymous denied
- product isolation
- customer isolation
- zero assignments
- partial assignments
- full capacity
- seat_limit sourced from selected effective entitlement row
- assigned_count never exceeds real assignment rows
- Post assignment does not appear in DM query
- suspended/removed membership behavior matches canonical Seat lifecycle
- no direct client table read is newly granted

Run existing Commercial Core regression/repro tests.

## H. Staging

After local tests PASS, apply ONLY this additive migration to Staging using the established safe Platform migration process.

Do not touch Production.

Run Staging tests using synthetic data and cleanup.

Prove:
- admin read works
- unauthorized read denied
- zero/partial/full states correct
- effective limit matches shared resolver
- assignment listing matches assign/release behavior
- cleanup leaves zero synthetic assignment/customer/member/entitlement residue where applicable

If Staging apply requires touching unrelated migrations or dirty state:
STOP and report blocker.

## I. Launcher impact

Do NOT implement Launcher UI.

Provide the exact future Launcher call contract:
RPC name
parameters
response fields
error cases

State how Launcher should combine this response with existing customer/member detail data.

## J. Safety

Do not modify Post or DM.
Do not implement Workbench UI.
Do not deploy Launcher.
Do not touch Production.
Do not alter existing Central Seat semantics.

Do not commit or push unless explicitly instructed.

## K. Final report

A. PREFLIGHT

B. EXISTING ADMIN/SEAT READ GAP

C. NEW READ CONTRACT
RPC name:
Parameters:
Return shape:
Authorization:
Seat limit source:
Assignment source:

D. FILES CHANGED

E. SECURITY TESTS

F. LOCAL REGRESSION

G. STAGING
MIGRATION_APPLIED = YES/NO
ADMIN_LIST_PASS = YES/NO
UNAUTHORIZED_DENIED = YES/NO
CUSTOMER_ISOLATION = PASS/FAIL
PRODUCT_ISOLATION = PASS/FAIL
SEAT_LIMIT_SAME_EFFECTIVE_ROW = PASS/FAIL
CLEANUP_ZERO_RESIDUE = YES/NO

H. CENTRAL SEAT INTEGRITY
ORIGINAL_SEAT_MIGRATION_SHA =
EXPECTED_SHA =
MATCH =
ASSIGN_SEMANTICS_CHANGED = NO
RELEASE_SEMANTICS_CHANGED = NO
STATUS_SEMANTICS_CHANGED = NO

I. LAUNCHER FUTURE CALL CONTRACT

J. REMOTE SAFETY
PRODUCTION_MUTATED = NO
POST_MODIFIED = NO
DM_MODIFIED = NO
LAUNCHER_UI_MODIFIED = NO

K. FINAL RESULT

If ready:

RESULT = PASS
ADMIN_SEAT_LISTING_READ_CONTRACT = PASS
STAGING_READY = YES
P3_BLOCKER_3_CLEARED = YES
READY_FOR_LAUNCHER_SEAT_UI = YES

If blocked:

RESULT = BLOCKED
ADMIN_SEAT_LISTING_READ_CONTRACT = BLOCKED
P3_BLOCKER_3_CLEARED = NO
BLOCKER = exact factual blocker

Finish this contract and stop.