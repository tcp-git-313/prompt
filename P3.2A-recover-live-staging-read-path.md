# P3.2A — Recover Live Staging Read Verification Path

ROLE: STAGING READ-PATH RECOVERY OWNER

MODE: READ-ONLY / NO IMPLEMENTATION

GOAL:
Unblock P3.2 by locating and proving the existing supported read-only verification path to the Central Supabase Staging project.

KNOWN STAGING PROJECT REF:
jlsqjvehwblkeuycjoyj

WORKSPACE ROOT:
F:\00-Ticenpi-SaaS

PRIMARY REPOS:
F:\00-Ticenpi-SaaS\ticenpi-platform
F:\00-Ticenpi-SaaS\Ticenpi-Launcher

REFERENCE REPOS IF NEEDED:
F:\00-Ticenpi-SaaS\TicenpiPost
F:\00-Ticenpi-SaaS\TicenpiDM

DO NOT modify source code.
DO NOT create migrations.
DO NOT apply migrations.
DO NOT change env/config.
DO NOT expose secrets.
DO NOT touch Production.
DO NOT install new global tools unless an existing project-local supported toolchain already requires it and installation is explicitly safe/reproducible; otherwise report the missing dependency instead.

## 1. Why this task exists

P3.2 stopped because the current checkout did not expose an obvious Live Staging verification path.

The blocker reported:

- no Supabase CLI/config.toml in the immediate Platform checkout
- no PostgreSQL client
- no configured PGHOST / TICENPI_BACKUP_PROJECT_REF in that shell
- therefore Live Staging schema/resolver could not be verified before adding the admin Seat listing read contract

However, earlier work already verified Central Seat on Staging project:

jlsqjvehwblkeuycjoyj

and Platform now owns copied canonical reproduction tooling, including:

F:\00-Ticenpi-SaaS\ticenpi-platform\supabase\repro\verify-staging-central-seat.py
F:\00-Ticenpi-SaaS\ticenpi-platform\supabase\repro\reproduce.ps1

Your task is to find the supported existing connection/verification path and prove it works read-only.

Do not invent a second ad-hoc Staging access method if an existing one already exists.

## 2. Preflight

Record for Platform and Launcher:

- branch
- HEAD
- git status --short
- git diff --stat

Do not clean or modify dirty WIP.

## 3. Search for existing Staging connection contract

Search the workspace READ-ONLY for references to:

jlsqjvehwblkeuycjoyj
SUPABASE_URL
SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY
SUPABASE_ACCESS_TOKEN
SUPABASE_DB_URL
DATABASE_URL
PGHOST
PGPORT
PGUSER
PGDATABASE
PGPASSWORD
TICENPI_BACKUP_PROJECT_REF
STAGING_SUPABASE
STAGING_PROJECT_REF
verify-staging-central-seat
supabase db
supabase link
psql
rpc
pg_get_functiondef
information_schema
pg_proc

Search:

- .env.example
- .env.template
- documented environment variable names
- PowerShell scripts
- Python verification scripts
- deploy scripts
- CI workflows
- handoff/docs
- existing local user-level config references
- project-local toolchain files

IMPORTANT:
Do not print actual secret values.
Only report:
- variable names
- file paths
- whether a value is present/usable in the current process
- connection method

If a secret is present, redact it completely.

## 4. Inspect canonical existing verification tooling first

Read and understand:

F:\00-Ticenpi-SaaS\ticenpi-platform\supabase\repro\verify-staging-central-seat.py

Determine:

- required env vars
- whether it performs writes
- whether it supports a pure read-only mode
- what Staging objects it validates
- whether it can verify:
  - function existence
  - function signatures
  - shared resolver behavior
  - product_seat_status availability
  - project identity

If it includes write/synthetic fixture behavior:
DO NOT RUN THE WRITE PORTION in this task.

Identify the exact safe read-only subset or command path.

## 5. Discover available clients/tools without changing the system

Check whether any of the following already exists and is callable:

- supabase CLI
- project-local Supabase CLI
- npx supabase already available through existing project dependencies
- psql
- Python libraries already used by the project
- PowerShell scripts already used for Staging verification
- curl / REST path already implemented by the repo
- existing MCP/connector-based Supabase access if explicitly configured in the project

Do not install a new global CLI just to make this pass.

If a project-local dependency can be used without changing lockfiles/package manifests, document it.

## 6. Preferred verification hierarchy

Use the first already-supported method that can safely prove Live Staging.

Priority:

1. Existing project verification script using existing configured credentials
2. Existing project-local Supabase CLI/config
3. Existing documented PowerShell/Python read-only Staging path
4. Existing read-only SQL connection already configured in environment
5. Existing authenticated REST/RPC path if it can prove the required facts
6. Otherwise BLOCKED with the exact missing credential/tool contract

Do not create new credentials.
Do not ask for service-role if a safer existing method already works.

## 7. Minimum proof required

You only need enough proof to unblock P3.2.

Prove:

STAGING_PROJECT_REF = jlsqjvehwblkeuycjoyj

And Live Staging contains the expected current Central Seat functions:

resolve_effective_entitlement_core(uuid)
effective_entitlements(uuid)
effective_entitlement_details(uuid)
product_seat_status(text)
assign_product_seat(uuid,text,uuid)
release_product_seat(uuid,text,uuid)

For each, capture only non-secret metadata:

- exists YES/NO
- argument signature
- return signature where retrievable
- schema
- project identity evidence

If the supported read-only path can safely retrieve function definitions, also prove both entitlement accessors depend on the shared resolver.

If definitions cannot be retrieved but signatures/project identity can be proven, report that limitation accurately.

## 8. No synthetic fixture in this task

Do NOT create the multiple-entitlement same-row fixture here.

This task is only to recover/prove the read path.

Once read access is proven, P3.2 or P3.1 can use the approved Staging test procedure separately.

## 9. Security rules

Never output:
- access token
- service-role key
- anon key
- database password
- full connection string with credentials
- JWT
- user password

Redact secrets as:
<REDACTED>

Do not copy secrets into files.
Do not add secrets to repo.
Do not echo full environment dumps.

## 10. Stop conditions

Stop and report BLOCKED if:

- the only available method requires creating a new secret/credential
- the only method would mutate Production
- the only method would modify Staging
- required existing credentials are absent from the current environment
- tooling would require changing project files/lockfiles
- Live project identity cannot be proven

Do not improvise around missing credentials.

## 11. Final report

### A. PREFLIGHT

Platform branch:
Platform HEAD:
Platform dirty:

Launcher branch:
Launcher HEAD:
Launcher dirty:

### B. EXISTING ACCESS CONTRACT DISCOVERY

Table:

Source/file | Method | Required env names | Available now YES/NO | Read-only capable YES/NO

### C. AVAILABLE TOOLING

supabase CLI:
project-local supabase:
psql:
python verifier:
PowerShell verifier:
other supported path:

### D. SELECTED READ PATH

Method:
Tool/script:
Required env names:
Secrets printed: NO
Project identity proof:

### E. LIVE STAGING PROOF

STAGING_PROJECT_REF =

Table:

Function | Exists | Args | Return | Shared resolver evidence

### F. P3.2 UNBLOCK STATUS

If sufficient:

RESULT = PASS
LIVE_STAGING_READ_PATH_RECOVERED = YES
STAGING_PROJECT_IDENTITY_VERIFIED = YES
CENTRAL_SEAT_FUNCTIONS_VISIBLE = YES
READY_TO_RESUME_P3_2 = YES

If insufficient:

RESULT = BLOCKED
LIVE_STAGING_READ_PATH_RECOVERED = NO
READY_TO_RESUME_P3_2 = NO
MISSING_TOOL_OR_CREDENTIAL_CONTRACT = <exact item>
RECOMMENDED_MINIMUM_NEXT_ACTION = <one smallest action only>

### G. SAFETY

SOURCE_FILES_MODIFIED = NO
CONFIG_MODIFIED = NO
SECRETS_WRITTEN = NO
STAGING_MUTATED = NO
PRODUCTION_MUTATED = NO
COMMIT_CREATED = NO
PUSHED = NO
DEPLOYED = NO

Finish verification and stop.
