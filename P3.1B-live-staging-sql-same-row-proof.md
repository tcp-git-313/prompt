# P3.1B — Live Staging Canonical SQL & Same-Effective-Row Proof

ROLE: CENTRAL SEAT LIVE STAGING VERIFICATION OWNER

MODE: STAGING VERIFICATION ONLY

GOAL:
After the approved Staging read path has been recovered, clear the remaining P3.1 Live Staging SQL and same-effective-row blockers.

PREREQUISITE:
P3.2A / Staging read-path recovery must report:

LIVE_STAGING_READ_PATH_RECOVERED = YES
STAGING_PROJECT_IDENTITY_VERIFIED = YES
READY_TO_RESUME_P3_2 = YES

If that prerequisite is not satisfied, STOP immediately.

STAGING PROJECT REF:
jlsqjvehwblkeuycjoyj

PLATFORM REPO:
F:\00-Ticenpi-SaaS\ticenpi-platform

DO NOT modify Production.
DO NOT modify product source.
DO NOT deploy Post or DM.
DO NOT change resolver logic.
DO NOT rewrite existing Central Seat migration.
DO NOT commit or push.

The only allowed remote mutation is the controlled synthetic Staging fixture in this prompt, with guaranteed cleanup.

## 1. Preflight

Record:
- Platform branch
- Platform HEAD
- git status --short
- git diff --stat

Verify this file still exists:

F:\00-Ticenpi-SaaS\ticenpi-platform\supabase\migrations\20260923_000600_central_product_seats.sql

Verify SHA-256 remains:

32EED072E422F576C9F5612BDD2E6E33B2F265A4D777B9BAC44AC2BE9A7B62CB

If SHA differs, STOP.

## 2. Prove Live Staging identity

Using only the recovered approved read path, prove the connected project is:

jlsqjvehwblkeuycjoyj

Do not proceed if project identity cannot be proven.

## 3. Verify live function signatures

Read-only verify Live Staging contains:

resolve_effective_entitlement_core(uuid)
effective_entitlements(uuid)
effective_entitlement_details(uuid)
product_seat_status(text)
assign_product_seat(uuid,text,uuid)
release_product_seat(uuid,text,uuid)

Required live signatures:

resolve_effective_entitlement_core(uuid)
→ product_code text, status text, seat_limit integer, entitlement_id uuid

effective_entitlements(uuid)
→ product_code text, status text

effective_entitlement_details(uuid)
→ product_code text, status text, seat_limit integer, entitlement_id uuid

product_seat_status(text)
→ jsonb

assign_product_seat(uuid,text,uuid)
→ jsonb

release_product_seat(uuid,text,uuid)
→ jsonb

Also verify entitlements.seat_limit is integer on Live Staging.

## 4. Verify shared resolver definition

Using live metadata/function definitions if available, prove:

effective_entitlements(uuid)
and
effective_entitlement_details(uuid)

both call the same:

resolve_effective_entitlement_core(uuid)

Do not modify definitions.

If the approved read path can prove signatures but cannot retrieve function bodies, report that limitation and STOP before the fixture unless the existing canonical verifier can safely prove the same-row result without depending on unverified function-body assumptions.

## 5. Controlled synthetic fixture

Before any mutation:

- choose a unique synthetic marker
- count any existing rows using that marker
- expected pre-test residue = 0
- identify every row/table that will be created
- confirm cleanup can remove all created rows

If pre-test residue is not zero, use a new unique marker.

Use only Staging.

Never use a real customer, real email, or real user identity.

Create the minimum synthetic commercial records required to evaluate the effective entitlement resolver.

For one synthetic customer/product create two entitlement candidates:

A:
status = active
seat_limit = 2
updated_at = an older deterministic timestamp

B:
status = trial
seat_limit = 5
updated_at = a newer deterministic timestamp

Both must be otherwise eligible under the existing resolver rules.

Do NOT alter resolver logic to make the fixture pass.

## 6. Required proof

Call the live old accessor:

effective_entitlements(customer_id)

Expected selected result:
- same product_code
- status = trial

Call the live detailed accessor:

effective_entitlement_details(customer_id)

Expected selected result:
- same product_code
- status = trial
- seat_limit = 5
- entitlement_id = B.id

Explicitly prove:

SAME_EFFECTIVE_ROW = YES

SEAT_LIMIT_FROM_SELECTED_ROW = YES

NOT_SUMMED = YES

NOT_INDEPENDENT_MAX = YES

The proof must show B is the selected row because of the existing effective resolver ordering, not because 5 is numerically larger.

## 7. Cleanup

Cleanup must run even on assertion/test failure.

Remove all synthetic records created by this task in safe dependency order.

After cleanup prove zero residue for the unique marker across every touched table.

If synthetic audit rows were created solely by the fixture and can be safely identified, remove or account for them according to the existing Staging test convention.

Do not delete unrelated audit history.

## 8. No Production work

Production remains outside this task.

Do not:
- change Production flags
- deploy Seat code
- disable tenant key
- inspect or mutate Production beyond already-known evidence

Production rollout remediation is a later workstream.

## 9. Final report

A. PREFLIGHT

Platform branch:
Platform HEAD:
Platform dirty:
Central Seat migration SHA:
MATCH = YES/NO

B. LIVE STAGING IDENTITY

STAGING_PROJECT_REF =
IDENTITY_PROVEN = YES/NO
READ_PATH_USED =

C. LIVE SQL

Table:
Function | Exists | Args | Return | Shared resolver evidence | PASS/FAIL

seat_limit live type:

D. FIXTURE

Synthetic marker:
Pre-test residue:
A entitlement:
- id
- status
- seat_limit
- updated_at

B entitlement:
- id
- status
- seat_limit
- updated_at

effective_entitlements result:
effective_entitlement_details result:

SAME_EFFECTIVE_ROW =
SEAT_LIMIT_FROM_SELECTED_ROW =
NOT_SUMMED =
NOT_INDEPENDENT_MAX =

E. CLEANUP

Table:
Object/table | Before | Created | After cleanup

CLEANUP_ZERO_RESIDUE = YES/NO

F. SAFETY

SOURCE_FILES_MODIFIED = NO
SQL_DEFINITION_MODIFIED = NO
PRODUCTION_MUTATED = NO
PRODUCTION_CONFIG_CHANGED = NO
STAGING_SCHEMA_CHANGED = NO
STAGING_TEMP_FIXTURE_CREATED = YES/NO
STAGING_TEMP_FIXTURE_CLEANED = YES/NO
COMMIT_CREATED = NO
PUSHED = NO
DEPLOYED = NO

G. FINAL RESULT

If complete:

RESULT = PASS
LIVE_STAGING_CANONICAL_SQL_VERIFIED = YES
SAME_EFFECTIVE_ROW_PROVEN = YES
SEAT_LIMIT_FROM_EFFECTIVE_ROW = YES
P3_1_STAGING_SQL_BLOCKER_CLEARED = YES
READY_TO_RESUME_P3_2 = YES
READY_FOR_PHASE_4_STAGING_IMPLEMENTATION = YES

If blocked:

RESULT = BLOCKED
LIVE_STAGING_CANONICAL_SQL_VERIFIED = NO
SAME_EFFECTIVE_ROW_PROVEN = NO
P3_1_STAGING_SQL_BLOCKER_CLEARED = NO
BLOCKER = exact factual blocker

Finish verification and stop.
